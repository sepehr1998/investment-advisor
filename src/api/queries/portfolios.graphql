query GetPortfoliosByContact($contactId: Long!) {
  portfoliosByParameters(parameters: { contactId: $contactId }) {
    id
    name
    currency {
      securityCode
    }
    status
    startDate
  }
}
