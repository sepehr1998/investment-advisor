query GetTransactionsByPortfolio($portfolioId: Long!) {
  transactions(portfolioId: $portfolioId) {
    id
    type {
      typeCode
      typeName
    }
    security {
      name
      securityCode
    }
    tradeDate
    amount
    quantity
    price
    currency {
      securityCode
    }
  }
}
